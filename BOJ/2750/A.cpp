#include <iostream>
#include <vector>
using namespace std;

#define MAX_NUM 1000

vector<int> arr;

void mergesort(int l, int r)
{
    int m = (l + r) / 2;
}

void merge()
{
}

int main()
{
    freopen("input.txt", "rt", stdin);
    int N;
    cin >> N;
    for (int i = 0; i < N; i++)
    {
        int temp;
        cin >> temp;
        arr.push_back(temp);
    }
}